# Core dependencies
pybind11>=3.0.0
numpy>=1.18.0

# Optional dependencies for examples and testing
matplotlib>=3.0.0
pandas>=1.0.0
scikit-learn>=0.24.0

# Development dependencies (only needed for building from source)
# cmake>=3.17.0  # System dependency, not pip installable
# setuptools>=45.0.0  # Usually included with Python